export interface IUserQuery {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    confirPassword: string;
};

export interface IUser {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
};
